// Map GraphQL enums to backend enums
export { Role } from "@/types";

// Export GraphQL enum resolvers here as needed.
// Keep default export as empty object to mirror NeuroVibes composition pattern.
const enums = {} as const;
export default enums;
