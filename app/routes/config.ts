export const publicRoutes: string[] = ["/", "/login", "/signup"];

export const protectedRoutes: string[] = [
  "/home",
  // add more protected paths here (e.g., "/budgets", "/accounts", "/profile")
];
